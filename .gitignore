# 虚拟环境 / IDE
.venv/
.venv-vllm/
.venv-train/
.idea/
.trae/
.vscode/
__pycache__/
*.pyc

# 工具缓存
.cargo/
unsloth_compiled_cache/

# 测试产物
.coverage
htmlcov/

# 涉密原始数据：本地保留，勿推远端（仅加密包例外）
data/*.jsonl
data/*.jsonl.gz
data/*.state.json
data/*.png
# 划分产物同样涉密（仅保留 split_manifest.json 统计清单）
data/split/*.jsonl
# 评测低分观测（含模型输出原文与用户对话，涉密）
obs_*.jsonl
data/compare/*.jsonl

# 训练产出（LoRA 适配器 / 合并权重）
models/

!data/user_chat_analysis.jsonl.gz.enc
