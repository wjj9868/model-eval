# user_chat_analysis 蒸馏训练全流程

> 目标：用线上 teacher（闭源模型）输出做 SFT 蒸馏 Qwen3.5-2B，产出可被 vLLM 托管、具备六字段 JSON 结构化分析能力的服务模型。
> 全流程：解密数据 → 清洗 → 划分 → 训练 → 评测 → 部署。

```
数据包(.enc) ──解密──> user_chat_analysis.jsonl (74,478 行)
    │
clean_train_data.py   自评剔除归因/结构缺陷样本 (~0.4%) ──> train_clean.jsonl (~74.2k)
    │
split_dataset.py      内容聚类防泄漏 + 分层划分 ──> data/split/{train, test_1k, dev}.jsonl
    │
train_lora.py         Unsloth LoRA SFT ──> lora 适配器 + merged_16bit 权重 + train_run.json
    │
run_real_eval.py      生成 + 评分器打分（口径对齐线上） ──> real_eval_scores.jsonl
    │
vLLM 部署             max_model_len=24576 + response_format(json_schema, strict)
```

---

## 0. 环境与口径基准（先读，防配错）

| 项 | 值 | 来源 |
|---|---|---|
| 训练环境 | GPU 服务器（参考 A10 23G）+ `.venv-train`（unsloth + trl + peft + transformers≤5.5.0） | — |
| 评分依赖 | sentence-transformers（bge-small-zh-v1.5，CPU 可跑） | — |
| **线上调用口径** | **单条 user 消息（无 system prompt，任务指令嵌在 prompt 模板头部）+ response_format(json_schema, strict=true) + temperature=1.0** | 核实自 bumblebee-server `UserChatAnalysisAI` / `OpenRouterTaskCaller` |
| 模型原生上下文 | 262,144（Qwen3.5-2B） | config.json |
| 训练序列长度 | 12288（覆盖 99.83%，超限剔除 0.17%，全量实测） | `stat_seq_len.py` |

**关键风险**：任何环节注入线上不存在的 system prompt、或评测温度偏离 1.0，都会造成训练/评测/生产三方分布失配，分数不可比。评测脚本已默认对齐，勿随意覆盖。

---

## 1. 数据准备

```bash
# 解密（密钥另发，勿进代码/历史；交互输入避免落 shell 历史）
openssl enc -d -aes-256-cbc -pbkdf2 -iter 200000 \
  -in data/user_chat_analysis.jsonl.enc -out data/user_chat_analysis.jsonl
# 校验：应为 74,478 行，每行 {"prompt", "output"}
wc -l data/user_chat_analysis.jsonl
```

- 数据为线上 raw 模型输出（`manage_model_log` 导出），**非** `manage_chat_analysis` 表（后者经 sanitize 修复，分数会虚高）。
- jsonl 含涉密内容：不进 git、不 cat、不外发；调试落盘文件用完即删。

## 2. 清洗（scripts/clean_train_data.py）

**目的**：teacher==student 自评，剔除归因错误样本（teacher 把人设发言记成用户事实，是已实证的 teacher 缺陷，不让 2B 学到）与结构缺陷样本。

```bash
python scripts/clean_train_data.py --reset --device cuda --workers 8
# 产出：data/train_clean.jsonl（保留样本）+ data/train_clean_report.jsonl（逐行分数，只含行号与分数）
```

- 判定规则：`speaker_attribution == 1.0`（None 视为不适用、不算错）且 `json_valid == 1.0`。
- 预期剔除率 **~0.4%**（归因 ~0.17% + 结构 ~0.22%），产出 ~74.2k 条。
- 性能：单进程 GPU ~48 行/s；`--workers 8` 后 ~67 行/s。若要进一步提速可开 MPS（`nvidia-cuda-mps-control -d`，预期 150-250 行/s）。
- **风险**：重跑必须 `--reset`，否则输出追加导致重复行；`--offset N` 可断点续跑。

## 3. 划分（scripts/split_dataset.py）

```bash
python scripts/split_dataset.py           # 默认输入已是 train_clean.jsonl
# 产出：data/split/train.jsonl / test_1k.jsonl / dev.jsonl + split_manifest.json
```

- **内容聚类防泄漏**：同会话/同记忆块的样本整簇同侧，杜绝"训练集背过测试集上下文"的分数虚高。
- 比例 ~98 : 1.4 : 0.7（test 1000 / dev 500）。74k 量级下按"决策需要多少样本"倒推，不按比例切（8:1:1 属过度投资，详见脚本头注释）。
- `--force-test-file data/eval_holdout_100.jsonl` 把旧基线样本固定在 test 最前，跨版本逐条可比。
- **铁律**：训练前一次性切好；test 只做最终裁决，反复拿 test 改配置会让它退化成 dev。

## 4. 训练（scripts/train_lora.py）

```bash
.venv-train/bin/python scripts/train_lora.py \
    --model Qwen/Qwen3.5-2B \
    --out-dir models/lora_2b --merged-dir models/merged_2b \
    --eval-file data/split/test_1k.jsonl --eval-limit 30 \
    --epochs 1
# 先小跑验证：加 --limit 2000 --max-steps 20
```

关键配置与依据：

| 配置 | 值 | 依据 |
|---|---|---|
| loss 掩码 | 仅 assistant 段（train_on_responses_only） | 不浪费算力复述超长 prompt |
| 长度过滤 | max_seq_len=12288，超限剔除（非截断） | 截断会截掉 response 本体，教模型生成残缺 JSON |
| NEFTune | α=5 | ICLR 2024，指令跟随显著提升 |
| rsLoRA | 开启 | rank≥16 更稳 |
| target modules | unsloth 自动选全部线性层 | "LoRA Learns Less and Forgets Less"（TMLR 2024）：须覆盖 MLP |
| lr / rank | 2e-4 / r16 α16 | LoRA 最优 LR ≈ 10× 全参，且与 rank 基本无关（Schulman 2025） |
| 调度 | cosine + warmup 0.05 + group_by_length | 长序列 SFT 标准 |
| bf16 + adamw_8bit + unsloth 梯度检查点 | — | 23G 单卡 2B 的显存组合 |

- 数据量上去后（>1 万条）：epochs 提到 2-3，接 eval + `load_best_model_at_end` 选 checkpoint；rank 视 loss 平台化程度升 32/64。
- 训练结束自动在留出集上跑真实评测（生成 + 仓库评分器），**不因 loss 低就交付**（loss 低但结构化输出崩掉是 SFT 典型失败模式）。
- 产物：LoRA 适配器（out-dir）、**合并 16bit 权重（merged-dir，供 vLLM 直接加载）**、`train_run.json`（超参/序列统计/留出集分数/评测口径，保证可复现）。

## 5. 评测（scripts/run_real_eval.py）

```bash
# 正式裁决：openai 后端（vLLM 服务），test 全量 1000 条
python scripts/run_real_eval.py --backend openai --base-url http://127.0.0.1:8000 \
    --input data/split/test_1k.jsonl --limit 1000 \
    --dump-outputs data/tmp_students.jsonl --out data/real_eval_scores.jsonl
# 打分可独立重跑（避免重复推理）：--stage score --dump-outputs 同一文件
```

- 口径默认对齐线上：无 system prompt、`response_format json_schema(strict)`、temperature=1.0、seed 可复现。
- **截断降级**：`finish_reason=length` 按调用失败计 0 分（与线上 `success=false` 同语义），报告单列截断率——schema 约束救不了 token 打满截断，这是唯一残余失败模式。
- **两条曲线都要看**：默认口径 = 部署等价质量（`json_valid` 会被 schema 兜成 1.0，信号在 L2 语义维度）；`--no-response-schema` = 裸模型能力。
- 指标体系：L1 规则（json_valid 四档）+ L2 语义（记忆 P/R、归因、幻觉、summary/intent/other）加权总分；None 分项按"不适用"剔除并重归一化。
- 打分多进程：`--workers N`（CPU，与 vLLM 抢显存时勿用 cuda）。
- `--dump-outputs` 落盘内容涉密，评测完删除。

## 6. 部署（vLLM + 线上接入）

```bash
vllm serve models/merged_2b \
    --max-model-len 24576 \
    # 其余按机器常规配置；response_format 由请求体携带，无需启动参数
```

| 项 | 要求 | 原因 |
|---|---|---|
| `--max-model-len` | **24576** | prompt 实测 max 21,391 + 生成 2,048 ≈ 23.4k，24,576 覆盖 100% |
| `response_format` | 请求级 json_schema(strict)，schema 用 `app/schemas/user_chat_response_schema.json`（与线上 DB 配置一致） | 线上 teacher 即此架构；训练数据是 schema 约束下产物，推理同构才不失配 |
| `max_tokens` | 2048（请求体已带） | teacher 输出 p99=739 / max=1,243，2048 覆盖 100% |
| `finish_reason=length` | 按失败降级重试 | 截断产出的合法前缀 JSON 不能入库 |

- **Java 侧零改动**：线上网关路径（`callViaGateway`）本就透传 schema，学生 vLLM 作为网关端点注册即可复用整套调用/降级语义。
- 线上 sanitize 二道防线（空值保旧、过期事件丢弃、超限截断）保持不动——它兜的是 schema 管不住的语义层。
- 上线前两项验证：① 训练渲染文本与 vLLM 服务端 `apply_chat_template` 输出做字符串 diff（确认 thinking 头/system 注入行为一致）；② 发一条带 schema 的请求确认约束真生效（个别版本会静默忽略 response_format）。

## 7. 回归验证

```bash
# 评分器全路径测试（90 项，纯逻辑、无需 MySQL）
.venv/Scripts/python.exe -m pytest tests/local_eval -q        # Windows
.venv/bin/python -m pytest tests/local_eval -q                # Linux

# 长度分布复核（换数据/换截断时重跑）
python scripts/stat_seq_len.py --input data/user_chat_analysis.jsonl
```

## 8. 常见问题

| 现象 | 根因 | 处理 |
|---|---|---|
| 清洗多进程只有 ~1.4 倍提速 | 每行一次 GPU 同步往返，8 上下文驱动层串行 | 开 MPS，或接受 ~67 行/s |
| 评测分数与历史不可比 | 口径漂移（system/温度/schema 开关） | 核对 `train_run.json` 的 `holdout_eval_config` 与 run_real_eval 启动打印 |
| 训练后 json_valid 反而低 | 长度过滤被绕过（截断而非剔除） | 确认走 `filter_by_length`，勿改回截断 |
| vLLM 部署分数骤降 | 模板渲染不一致 / schema 被静默忽略 | 做第 6 节的两项上线验证 |
| 旧基线对不上 | 用了旧模板样本（无六字段 schema） | 评测加 `--require-schema`，或以 `test_1k.jsonl` 前置强制样本对齐 |
